@inherits LayoutComponentBase
@using Microsoft.AspNetCore.Components.Authorization
@inject NavigationManager Navigation

<div class="container-fluid">
    <AuthorizeView>
        <Authorized>
            <nav class="navbar navbar-expand-lg navbar-light bg-light">
                <a class="navbar-brand" href="#">Admin Dashboard</a>
                <div class="collapse navbar-collapse">
                    <ul class="navbar-nav me-auto">
                        <!-- Add other navbar items here if needed -->
                    </ul>
                    <ul class="navbar-nav ms-auto">
                        <li class="nav-item">
                            <span class="navbar-text">
                                Hello, @context.User.Identity?.Name!
                            </span>
                        </li>
                        <li class="nav-item">
                            <button class="btn btn-danger ms-2" type="button" @onclick="Logout">Log Out</button>
                        </li>
                    </ul>
                </div>
            </nav>
        </Authorized>
    </AuthorizeView>

    <div class="row">
        <AuthorizeView>
            <Authorized>
                <div class="col-md-2 bg-light sidebar">
                    <ul class="nav flex-column">
                        <li class="nav-item">
                            <NavLink class="nav-link" href="" Match="NavLinkMatch.All">Home</NavLink>
                        </li>
                        <li class="nav-item">
                            <NavLink class="nav-link" href="users">Users</NavLink>
                        </li>
                        <li class="nav-item">
                            <NavLink class="nav-link" href="posts">Manage Posts</NavLink>
                        </li>
                        <li class="nav-item">
                            <NavLink class="nav-link" href="settings">Settings</NavLink>
                        </li>
                    </ul>
                </div>
            </Authorized>
        </AuthorizeView>

        <div class="col-md-10">
            @Body
        </div>
    </div>
</div>

@code {
    private void Logout()
    {
        Navigation.NavigateTo("/logout", true);
    }
}
